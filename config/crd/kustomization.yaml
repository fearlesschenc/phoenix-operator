# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/tenant.phoenix.fearlesschenc.com_workspaces.yaml
- bases/tenant.phoenix.fearlesschenc.com_clusters.yaml
- bases/workload.phoenix.fearlesschenc.com_applications.yaml
- bases/workload.phoenix.fearlesschenc.com_offlineapplications.yaml
- bases/repo.phoenix.fearlesschenc.com_clusterrepoes.yaml
- bases/repo.phoenix.fearlesschenc.com_applicationrepoes.yaml
- bases/tenant.phoenix.fearlesschenc.com_workspaceclaims.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_workspaces.yaml
#- patches/webhook_in_clusters.yaml
#- patches/webhook_in_applications.yaml
#- patches/webhook_in_offlineapplications.yaml
#- patches/webhook_in_clusterrepoes.yaml
#- patches/webhook_in_applicationrepoes.yaml
#- patches/webhook_in_workspaceclaims.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_workspaces.yaml
#- patches/cainjection_in_clusters.yaml
#- patches/cainjection_in_applications.yaml
#- patches/cainjection_in_offlineapplications.yaml
#- patches/cainjection_in_clusterrepoes.yaml
#- patches/cainjection_in_applicationrepoes.yaml
#- patches/cainjection_in_workspaceclaims.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
